# Copilot Instructions

`AGENTS.md` at the repository root is the source of truth for repo-wide guidance. Follow it first and avoid duplicating those instructions here.

Copilot-specific note:

- If a project folder contains a closer `GEMINI.md` or other local guidance file, treat the closest project-local instructions as more specific than the root baseline.
