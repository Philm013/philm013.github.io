# GEMINI.md

Use `AGENTS.md` in the repository root as the repo-wide source of truth.

This file intentionally stays thin to avoid instruction drift. Add Gemini-specific guidance here only when it cannot live in `AGENTS.md` or a reusable `SKILL.md`.
